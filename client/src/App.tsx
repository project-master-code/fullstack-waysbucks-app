import './App.css';
import { AppRouter } from './routes';

function App() {
  // const dispatch = useAppDispatch();
  // React.useEffect(() => {
  //   dispatch(checkAuth());
  // }, []);
  // console.log('check', checkAuth);

  return <AppRouter />;
}

export default App;
